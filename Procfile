web: python3 odoo/odoo-bin \
  --http-port=${PORT} \
  --db_host=${PGHOST} --db_port=${PGPORT} --db_user=${PGUSER} --db_password=${PGPASSWORD} --db_name=${PGDATABASE} \
  --addons-path=addons,odoo/addons \
  --admin-passwd=${ODOO_ADMIN_PASSWORD}
